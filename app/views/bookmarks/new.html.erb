<h1>Add a new movie</h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie, prompt: "Add a movie", label_method: :title %>
  <%= f.input :comment, placeholder: 'Enter comment' %>
  <%= f.button :submit %>
<% end %>
